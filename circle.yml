machine:
  python:
    version: 2.7.13
  node:
    version: 8.1.2

deployment:
  development:
    branch: master
    commands:
      - npm run deploy:ci:dev
  production:
    branch: production
    commands:
      - npm run deploy:ci:prod

